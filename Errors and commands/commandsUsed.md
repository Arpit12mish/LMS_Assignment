# Navigation (native-stack is fastest)
npx expo install @react-navigation/native @react-navigation/native-stack
npx expo install react-native-screens react-native-safe-area-context

# Video player
npx expo install expo-av

# custom controls for the video (seek, skip, mute)
npx expo install @react-native-community/slider

# Storage (For one time Pop up message - modal)
npx expo install @react-native-async-storage/async-storage

# Sentry (crash/perf) + Amplitude (product analytics)
npx expo install sentry-expo
npx expo install @amplitude/analytics-react-native
